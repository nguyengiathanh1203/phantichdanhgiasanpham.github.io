#  Cập nhật file khi làm việc trên Codespaces (VSCode Web):

git add .

git commit -m "Update file"

git push origin main

#  Cập nhật trước khi push lên Codespaces (VSCode Web):

git fetch origin

git merge origin/main
